@using Bakery.Models;


<h2>Orders</h2>
@Html.ActionLink("Add a new order", "Create")


@if (@Model.Count == 0)
{
    <h4>No orders have been placed on your account.</h4>
}


@foreach (Order o in Model)
{
    @Html.ActionLink($"{o.Treat}s", "Details", new { id = o.OrderId })
}
