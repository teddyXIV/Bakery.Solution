@using Bakery.Models;


<h2>Flavors</h2>


@if (@Model.Count == 0)
{
    <h4>No flavors have been added.</h4>
}

<ul>
    @foreach (Flavor f in Model)
    {
        <li>@Html.ActionLink($"{f.Name}", "Details", new { id = f.FlavorId })</li>
    }
</ul>

@Html.ActionLink("Add a new flavor", "Create")